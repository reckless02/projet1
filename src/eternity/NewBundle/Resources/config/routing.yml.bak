eternitynewBundle_homepage:
    pattern:  /hello/{name}
    defaults: { _controller: eternityNewBundle:Homepage:index }

eternitynewBundle_contact:
    pattern: /hello/views/contact/contact.html.twig{name}
    defaults: { _controller: eternityNewBundle:contact:contact }
